package Argument;

public class Garage {
  //we need to declare the kinds of object, we cannot put other object as a parameter here.
  public void Park(Car car) {
    System.out.println(car.name +" is parked in the garage");
  }
}
